va_commands.register_command("select_constructor", {
    description = "Select Constructor",
    execute = function(player)
        -- Logic to select the idle Constructor unit for the player
    end,
})